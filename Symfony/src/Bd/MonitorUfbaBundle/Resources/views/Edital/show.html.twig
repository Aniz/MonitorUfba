{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Edital</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Publicacao</th>
                <td>{{ entity.publicacao }}</td>
            </tr>
            <tr>
                <th>Arquivo</th>
                <td>{{ entity.arquivo }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('edital') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('edital_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
